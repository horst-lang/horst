class Animal {
    speak() {
        this.name = "Animal";
    }
}

class Dog < Map {
    speak(text) {
        print "Dog speaking: " + text;
    }
}

let dog = Dog();
dog.set("name", "Dog");
dog.speak("Woof!");
print dog.get("name");