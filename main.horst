class AppendX {
    init(string) {
        this.string = string
    }
    toString() {
        return List(this.string, "x").toString()
    }
}

let list = List("a", AppendX("x"), List("c", "d", List("e", "f", List("g", "h", List("i", 1)))))
print list.toString()

print fetch("http://httpbin.org/get");